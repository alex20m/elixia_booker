# Secrets — never commit
.env
.env.*
!.env.example
.dev.vars
.dev.vars.*
.env.local
.env*.local

# Leftovers from the (removed) discovery harness — these hold live session
# cookies, so keep ignoring them even though nothing produces them any more.
*.har
auth-state.json
captures/

# Deps / build
node_modules/
dist/
.next/
out/
.vercel/
coverage/
next-env.d.ts
*.tsbuildinfo

# OS / editor
.DS_Store
*.log
.env*
